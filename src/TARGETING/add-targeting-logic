MAYBE EVEN YOU CAN MAKE ONE OBJECT THAT DOES BOTH, ASSIGN TARGET AND CREATE RULES


default - things like MSPs, MPs, local council; with logic that searches by postcode
custom - for things that aren't a simple custom but not 'repeatable' ones; i.e. a particular committee (idk man, not a massively important distinction)